services:
  # Postgres (replaces previous MongoDB)
  postgres:
    image: postgres:15
    container_name: eventora_postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - app-network

  # API Gateway
  api-gateway:
    build: ./api-gateway
    container_name: api_gateway
    environment:
      - PORT=${API_GATEWAY_PORT}
      - AUTH_SERVICE_URL=${AUTH_SERVICE_URL}
      - USER_SERVICE_URL=${USER_SERVICE_URL}
      - NODE_ENV=${NODE_ENV}
    ports:
      - "${API_GATEWAY_PORT}:${API_GATEWAY_PORT}"
    depends_on:
      - auth-service
      - user-service
    networks:
      - app-network

  # Auth Service
  auth-service:
    build: ./auth-service
    container_name: auth_service
    environment:
      - AUTH_SERVICE_PORT=${AUTH_SERVICE_PORT}
      - JWT_SECRET=${JWT_SECRET}
      - USER_SERVICE_URL=${USER_SERVICE_URL}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      - GOOGLE_REDIRECT_URI=${GOOGLE_REDIRECT_URI}
      - NODE_ENV=${NODE_ENV}
      - FRONTEND_URL=${FRONTEND_URL}
    ports:
      - "${AUTH_SERVICE_PORT}:${AUTH_SERVICE_PORT}"
    depends_on:
      - user-service
    networks:
      - app-network

  # User Service
  user-service:
    build: ./user-service
    container_name: user_service
    environment:
      - PORT=${USER_SERVICE_PORT}
      - DB_CONNECTION=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}
      - BCRYPT_SALT_ROUNDS=${BCRYPT_SALT_ROUNDS}
      - NODE_ENV=${NODE_ENV}
    ports:
      - "${USER_SERVICE_PORT}:${USER_SERVICE_PORT}"
    depends_on:
      - postgres
    networks:
      - app-network

  # Frontend
  frontend:
    build: ./frontend
    container_name: auth_frontend
    environment:
      - REACT_APP_API_URL=${API_GATEWAY_URL}
      - REACT_APP_NODE_ENV=${NODE_ENV}
      - VITE_API_URL=${API_GATEWAY_URL}
    ports:
      - "${FRONTEND_PORT}:3000"
    depends_on:
      - api-gateway

volumes:
  postgres_data:

networks:
  app-network:
    driver: bridge